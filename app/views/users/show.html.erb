<h2>ユーザー詳細画面</h2>

  <p id="notice"><%= notice %></p>
  <h1><%= @user.name %>のページ</h1>
  <p>メールアドレス: <%= @user.email %></p>
  <%= link_to '新しく投稿する', new_post_path %>
  <%# この行に編集画面へのリンクを追加する必要がある？ %>
  <br>
  <%= link_to '投稿一覧', posts_path %>
